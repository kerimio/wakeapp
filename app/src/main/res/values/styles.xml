<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here.
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item> -->
    </style>

    <style name="wakeapp_theme" parent="AppTheme" >
        <item name="colorPrimary">@color/pink_logo_wakeapp</item>
        <item name="colorPrimaryDark">@color/pink_logo_wakeapp</item>
        <item name="android:navigationBarColor">@android:color/background_dark</item>
        <item name="android:colorForeground">@color/orange_logo_wakeapp</item>
        <item name="android:statusBarColor">@android:color/background_dark</item>
        <item name="colorAccent">@color/orange_logo_wakeapp</item>
    </style>

</resources>
